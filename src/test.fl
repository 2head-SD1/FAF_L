setq x:Int plus(1, 2);
setq y:Int minus(2, 5);
plus(y,plus(minus(x, 3), 2));
define myFunc(x:Int, y:String)->Int {if true then false else true};

struct Date(day:Int, Month:Int, Year:Int);
struct Worker(Name:String, Birthdate:<Date>, Salary:Int);
setq fileContext:String readline;
define get_worker(data:String) -> <Worker>{Worker(/*Somehow make extraction of data*/)};
define get_workers(line:String) -> [<Worker>]{if d.d
                                              then empty
                                              else true
                                                    /*cons (get-worker (car (split line " "))) (get-workers (read-line line)))*/}
